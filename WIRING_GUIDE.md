# 🔌 硬件接线指南 (Hardware Wiring Guide)

本文档详细记录了 **智能胶囊分配器** 项目中所有硬件模块与 Raspberry Pi 5 的连接方式。

**最后更新日期：** 2025-12-02
**主控板：** Raspberry Pi 5

---

## 1. 舵机模块 (Servo Motor)
**型号：** MG996R (高扭矩数字舵机)
**供电要求：** 必须使用 **独立 6V 电源**，严禁直接由 Pi 供电。

| 舵机线色 | 功能 | 连接目标 | 说明 |
| :--- | :--- | :--- | :--- |
| 🟠 **橙/黄** | PWM 信号 | **GPIO 18** (Pin 12) | 硬件 PWM 控制引脚 |
| 🔴 **红** | VCC (+) | **外部电源正极 (6V)** | **不要接 Pi 的 5V** |
| 🟤 **棕** | GND (-) | **外部电源负极** | **必须与 Pi 共地** |

> **⚠️ 共地 (Common Ground)：** 外部电源的负极必须同时连接到舵机的棕线 **和** Raspberry Pi 的任意 GND 引脚 (如 Pin 6, 9, 14 等)，否则控制信号无效。

---

### 2. 指纹模块 (DY-50 / R307)
**通信协议**: UART (Serial)
**关键注意**:
*   **交叉连接**: 传感器的 TX 接树莓派的 RX，传感器的 RX 接树莓派的 TX。
*   **端口选择 (Pi 5)**: 在 Raspberry Pi 5 上，GPIO 14/15 对应的硬件串口是 **`/dev/ttyAMA0`**。请勿使用 `/dev/serial0`，因为它可能指向错误的端口。

| 传感器引脚 | 树莓派引脚 (Physical) | 树莓派 GPIO | 功能 |
| :--- | :--- | :--- | :--- |
| VCC (红) | Pin 1 | 3.3V Power | 供电 (3.3V) |
| GND (黑) | Pin 6 | Ground | 接地 |
| TX (绿/黄) | Pin 10 | GPIO 15 (RXD) | 数据接收 |
| RX (白/蓝) | Pin 8 | GPIO 14 (TXD) | 数据发送 |
| WAK (选) | - | - | 唤醒 (暂不使用) |
| VT (选) | - | - | 触摸感应 (暂不使用) |

---

## 3. Raspberry Pi 5 GPIO 引脚图示 (部分)

```text
       [SD Card Slot End]
       
       3.3V (Pin 1)  [●] [●]  (Pin 2)  5V
 [指纹 3v3] ->      |   |
       GPIO 2 (Pin 3)  [●] [●]  (Pin 4)  5V
       GPIO 3 (Pin 5)  [●] [●]  (Pin 6)  GND  <- [指纹 gnd]
       GPIO 4 (Pin 7)  [●] [●]  (Pin 8)  GPIO 14 (TXD) <- [指纹 rx]
          GND (Pin 9)  [●] [●]  (Pin 10) GPIO 15 (RXD) <- [指纹 tx]
      GPIO 17 (Pin 11) [●] [●]  (Pin 12) GPIO 18 (PWM) <- [舵机 信号]
      GPIO 27 (Pin 13) [●] [●]  (Pin 14) GND
      GPIO 22 (Pin 15) [●] [●]  (Pin 16) GPIO 23
 [指纹 t-3v3]->(Pin 17) [●] [●]  (Pin 18) GPIO 24
             ...
```

![](https://www.raspberrypi.com/documentation/computers/images/GPIO-Pinout-Diagram-2.png?hash=df7d7847c57a1ca6d5b2617695de6d46)

---

## 4. 软件配置检查

### 4.1 启用硬件 PWM (舵机)
检查 `/boot/firmware/config.txt` 是否包含：
```ini
dtoverlay=pwm,pin=18,func=2
```

### 4.2 启用串口 (指纹)
确保 Raspberry Pi 的串口未被控制台占用：
1. 运行 `sudo raspi-config`
2. 选择 `Interface Options` -> `Serial Port`
3. **Login shell:** 选择 **No**
4. **Serial port hardware:** 选择 **Yes**
5. 重启生效。
