# 🔌 硬件接线指南 (Hardware Wiring Guide)

本文档详细记录了 **智能胶囊分配器** 项目中所有硬件模块与 Raspberry Pi 5 的连接方式。

**最后更新日期：** 2025-12-02
**主控板：** Raspberry Pi 5

---

## 1. 舵机模块 (Servo Motor)
**型号：** SG90 (Micro Servo 9g) x 4
**供电要求：** 4.8V - 6V。
*建议使用外部 5V 电源为 4 个舵机供电，并与 Pi 共地。*

| 舵机编号 | 信号线 (橙) | PWM 通道 | 物理引脚 (Pin) |
| :--- | :--- | :--- | :--- |
| **Servo 1** | **GPIO 18** | PWM 2 | Pin 12 |
| **Servo 2** | **GPIO 12** | PWM 0 | Pin 32 |
| **Servo 3** | **GPIO 13** | PWM 1 | Pin 33 |
| **Servo 4** | **GPIO 19** | PWM 3 | Pin 35 |

| 通用线色 | 功能 | 连接目标 |
| :--- | :--- | :--- |
| 🔴 **红** | VCC (+) | **5V 电源** |
| 🟤 **棕** | GND (-) | **GND** (必须共地) |

> **⚠️ 系统配置：** 必须在 `/boot/firmware/config.txt` 中启用所有 PWM 引脚。
> 添加：
> `dtoverlay=pwm,pin=12,func=0`
> `dtoverlay=pwm,pin=13,func=0`
> `dtoverlay=pwm,pin=18,func=2`
> `dtoverlay=pwm,pin=19,func=2`

---

## 2. 显示屏 (IPS 1.3" ST7789)
**型号：** ZJY-IPS130-V2.0 (240x240)
**接口：** SPI (7-Pin)

| 屏幕引脚 | 功能 | 树莓派引脚 (Physical) | BCM (GPIO) | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| **GND** | 地 | **Pin 20** | GND | 共地 |
| **VCC** | 电源 | **Pin 17** | 3.3V | **注意接 3.3V** |
| **SCL** | SPI 时钟 | **Pin 23** | GPIO 11 (SCLK) | SPI0 SCLK |
| **SDA** | SPI 数据 | **Pin 19** | GPIO 10 (MOSI) | SPI0 MOSI |
| **RES** | 复位 | **Pin 22** | GPIO 25 | 复位信号 |
| **DC** | 数据/命令 | **Pin 18** | GPIO 24 | Data/Command |
| **BLK** | 背光 | **Pin 13** | GPIO 27 | 高电平亮 |

> **注意**: 此屏幕没有 CS 引脚，直接使用 SPI0 即可。

---

### 3. 指纹模块 (DY-50 / R307)
**通信协议**: UART (Serial)
**关键注意**:
*   **交叉连接**: 传感器的 TX 接树莓派的 RX，传感器的 RX 接树莓派的 TX。
*   **端口选择 (Pi 5)**: 在 Raspberry Pi 5 上，GPIO 14/15 对应的硬件串口是 **`/dev/ttyAMA0`**。请勿使用 `/dev/serial0`，因为它可能指向错误的端口。

| 传感器引脚 | 树莓派引脚 (Physical) | 树莓派 GPIO | 功能 |
| :--- | :--- | :--- | :--- |
| VCC (红) | Pin 1 | 3.3V Power | 供电 (3.3V) |
| GND (黑) | Pin 6 | Ground | 接地 |
| TX (绿/黄) | Pin 10 | GPIO 15 (RXD) | 数据接收 |
| RX (白/蓝) | Pin 8 | GPIO 14 (TXD) | 数据发送 |
| WAK (选) | - | - | 唤醒 (暂不使用) |
| VT (选) | - | - | 触摸感应 (暂不使用) |

---

## 3. Raspberry Pi 5 GPIO 引脚图示 (部分)

```text
       [SD Card Slot End]
       
       3.3V (Pin 1)  [●] [●]  (Pin 2)  5V
 [指纹 3v3] ->      |   |
       GPIO 2 (Pin 3)  [●] [●]  (Pin 4)  5V
       GPIO 3 (Pin 5)  [●] [●]  (Pin 6)  GND  <- [指纹 gnd]
       GPIO 4 (Pin 7)  [●] [●]  (Pin 8)  GPIO 14 (TXD) <- [指纹 rx]
          GND (Pin 9)  [●] [●]  (Pin 10) GPIO 15 (RXD) <- [指纹 tx]
      GPIO 17 (Pin 11) [●] [●]  (Pin 12) GPIO 18 (PWM) <- [舵机 信号]
      GPIO 27 (Pin 13) [●] [●]  (Pin 14) GND
      GPIO 22 (Pin 15) [●] [●]  (Pin 16) GPIO 23
 [指纹 t-3v3]->(Pin 17) [●] [●]  (Pin 18) GPIO 24
             ...
```

![](https://www.raspberrypi.com/documentation/computers/images/GPIO-Pinout-Diagram-2.png?hash=df7d7847c57a1ca6d5b2617695de6d46)

---

## 4. 软件配置检查

### 4.1 启用硬件 PWM (舵机)
检查 `/boot/firmware/config.txt` 是否包含：
```ini
dtoverlay=pwm,pin=18,func=2
```

### 4.2 启用串口 (指纹)
确保 Raspberry Pi 的串口未被控制台占用：
1. 运行 `sudo raspi-config`
2. 选择 `Interface Options` -> `Serial Port`
3. **Login shell:** 选择 **No**
4. **Serial port hardware:** 选择 **Yes**
5. 重启生效。
